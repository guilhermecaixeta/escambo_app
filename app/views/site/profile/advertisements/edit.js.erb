on_request_complete(() => {
    document
        .getElementById("main_section")
        .innerHTML = '<%= j render "site/profile/advertisements/form" %>'

    var editor = new FroalaEditor('#advertisement_description');
});
